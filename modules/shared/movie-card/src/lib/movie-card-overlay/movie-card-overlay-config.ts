import { ConnectedPosition } from '@angular/cdk/overlay';

/** The positions for the overlay. */
export const OVERLAY_POSITIONS: ConnectedPosition[] = [
  {
    originX: 'center',
    originY: 'center',
    overlayX: 'center',
    overlayY: 'center',
  },
];

/** The scaling factor for the overlay. */
export const OVERLAY_SCALING_FACTOR = 1.2;
